# ปัญหาการแสดงผลเมนู Branches บน Production (Vercel)

## 📌 อาการที่พบ (Symptom)
* หลังจากเขียนโค้ดเพื่อสร้างหน้าจัดการ **"สาขา (Branches)"** ในระบบ Admin (Local) เสร็จสิ้น ได้ทำการสั่ง Deploy โค้ดผ่าน Vercel CLI (ด้วยคำสั่ง `vercel --prod`)
* แต่เมื่อเข้าไปตรวจสอบที่ Domain หลักของคุณคือ `https://odm-vidwa-admin.vercel.app/admin` กลับพบว่าหน้าเว็บยังคงเหมือนเดิม **ไม่ปรากฏเมนู Branches ใหม่ที่เพิ่งทำเสร็จ**

---

## 🔍 สาเหตุหลักของปัญหา (Root Cause)
1. **การผูกโปรเจกต์ของ Vercel (Project Linkage):** 
   * ตรวจสอบด้วยคำสั่ง `vercel project ls` พบว่าก่อนหน้านี้ในเครื่องคอมพิวเตอร์ของคุณ โปรเจกต์ `admin` (ที่โฟลเดอร์ `c:\atgv\odm\admin`) ถูกผูก (Link) ติดไว้กับโปรเจกต์เก่าที่ชื่อว่า **`joy-admin`** บน Vercel
   * ทำให้ตอนที่สั่งคำสั่ง `vercel --prod` เพื่อส่งโค้ดขึ้นเซิร์ฟเวอร์ โค้ดที่อัปเดตใหม่ทั้งหมดจึงถูกส่งไป Build และตั้งเป็น Production อยู่ที่เว็บ **`joy-admin-ten.vercel.app`** แทนที่จะเป็น `odm-vidwa-admin.vercel.app` ทันที

2. **ขาดการเชื่อมต่อกับ GitHub แบบอัตโนมัติ:**
   * โปรเจกต์หลักของคุณไม่ได้ถูก Deploy ผ่านการกด Commit & Push จาก Git ไปยัง GitHub ตรงๆ แต่ต้องอาศัยส่งโค้ดผ่าน Vercel CLI ทำให้การชี้เป้าหมายที่ผิด (Link ผิดโปรเจกต์) นำไปสู่ปัญหาด้านบน 

---

## ✅ วิธีการแก้ไข (Solution)
เพื่อให้คำสั่ง Vercel CLI ส่งโค้ดขึ้นถูกโปรเจกต์ ได้ดำเนินการแก้ไขดังนี้:

1. **เชื่อมต่อโปรเจกต์ใหม่ให้ถูกต้อง:** 
   * รันคำสั่ง `vercel link --project odm-vidwa-admin --yes` ในโฟลเดอร์งานของคุณ เพื่อลบการเชื่อมต่อกับ `joy-admin` เดิม และผูก (Link) ชุดโค้ดปัจจุบันเข้ากับโปรเจกต์ `odm-vidwa-admin` ให้มั่นใจว่าชี้ถูกเป้า
2. **ดึง Environment Variables ใหม่:**
   * การ Link ตัวใหม่ จะไปทำการเขียนทับตัวแปรระบบ (`.env.local`) ให้ตรงกับเซิร์ฟเวอร์ `odm-vidwa-admin` เพื่อให้แน่ใจว่าค่า Token/Database ตรงกัน
3. **สั่ง Deploy โปรเจกต์ที่ถูกต้องขึ้นเป็น Production:**
   * รันคำสั่ง `vercel --prod --yes` เพื่อทำการ Build โค้ดและนำขึ้นสู่ Domain หลักของคุณ

เมื่อการ Deploy เสร็จสมบูรณ์ (รอ Vercel build ประมาณ 1 นาที) เมนู **"Branches"** ได้แสดงผลที่หน้าจอและพร้อมใช้งานบน Production ทันทีครับ 🎉
